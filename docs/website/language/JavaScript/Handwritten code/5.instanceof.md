# 手写 instanceOf 函数

```javascript
function instanceOf(target, type) {
  type = type.prototype
  target = target.__proto__
  while (true) {
    if (target === null) {
      return false
    }
    if (target === type) {
      return true
    }
    target = target.__proto__
  }
  return false
}
```
